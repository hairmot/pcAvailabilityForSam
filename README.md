very simple table - uses bootstrap and jquery CDN. - and React!

change the contents of the div #dataUrl in index.html to change the address of the data api.

to build run:

npm update<br>
npm run build
